## Variables & Data Types

## What is a Variable?
int x;     // declares the variable → tells C++ "reserve memory space for a whole number"
x = 5;     // assigns the value → puts 5 into that reserved space

int x = 5; // declare + assign in one line (cleaner way to do it)

## Arithmetic with Variables
int x = 5;
int y = 10;
int sum = x + y;        // calculates the sum and stores it in sum
std::cout << sum;       // prints the result → 15

## Data Types

// int → stores whole numbers
int age = 21;
int year = 2023;

// double → stores numbers with decimals
double price = 10.99;
double gpa = 2.5;

// char → stores a single character (always use single quotes)
char grade = 'A';
char initial = 'B';

// bool → stores only true or false
bool student = false;
bool power = true;

// string → stores a sequence of text (always use double quotes)
std::string name = "Bro";